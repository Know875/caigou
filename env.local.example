# 本地开发环境配置
# 复制此文件为 .env.local 并修改配置

# 数据库 (密码中的 @ 需要编码为 %40，端口使用 5432)
# 请替换为您的实际数据库密码
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD%40@localhost:5432/egg_purchase?schema=public

# Redis
# 方式一：使用 REDIS_URL（如果 Redis 有密码，格式为：redis://:password@host:port）
REDIS_URL=redis://localhost:6379
# 方式二：使用单独配置（如果 REDIS_URL 未设置）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# MinIO (可选，如果不需要文件存储可以暂时不配置)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
S3_ENDPOINT=http://localhost:9000
# MinIO 公共访问地址（用于生成签名 URL，解决 CORS 问题）
# 如果前端通过局域网 IP 访问（如 http://26.26.26.1:8080），这里应该设置为对应的 MinIO 地址
# 例如：MINIO_PUBLIC_ENDPOINT=http://26.26.26.1:9000
MINIO_PUBLIC_ENDPOINT=http://localhost:9000

# JWT (生产环境必须使用强密钥，至少32个字符)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# API
API_PORT=8081
NODE_ENV=development
TZ=Asia/Shanghai
CRON_TZ=Asia/Shanghai

# Web
NEXT_PUBLIC_API_URL=http://localhost:8081

# 钉钉机器人（可选）
# DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN

# OCR配置（可选）
# OCR_SPACE_API_KEY=your-ocr-space-api-key
# XFYUN_APP_ID=your-xfyun-app-id
# XFYUN_API_KEY=your-xfyun-api-key
# XFYUN_API_SECRET=your-xfyun-api-secret
